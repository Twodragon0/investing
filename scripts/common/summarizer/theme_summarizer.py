"""Composed ``ThemeSummarizer`` class wiring the mixins together.

Populated in step 6 of ``.omc/plans/split-summarizer.md``. Until then the
canonical class still lives in ``scripts/common/summarizer_legacy.py`` and
is re-exported by the package ``__init__``.
"""
